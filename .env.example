# GLOF Early Warning System — Environment Variables
# Copy this to .env and adjust values as needed

# JWT
JWT_SECRET_KEY=glof_himalaya_secret_2024

# Flask
FLASK_ENV=production

# MongoDB (only change if running externally)
MONGO_URI=mongodb://mongodb:27017/glof_db

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Simulator
BACKEND_URL=http://backend:5000
INTERVAL_SECONDS=5
